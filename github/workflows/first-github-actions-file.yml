name: Run shell commands
on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
      steps:
        - name: Echo a String
          run: echo "Hello World, this is my first Github Actions file"
  

